spring:
  application:
    name: DatabaseMeta
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/watererp
    username: test
    password: test
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
